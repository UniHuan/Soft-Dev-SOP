# 📜 软件著作权申请全流程 SOP

> **适用对象**: Claude Code (AI Agent) 辅助执行 + 用户操作
> **目标**: 从零到拿到软著登记证书，全流程可追溯、不出错、不返工
> **适用类型**: 中国国家版权局计算机软件著作权登记 (App/小程序/Web/后端/桌面)
> **办理周期**: 受理后约 30 个工作日 (自然人免费，企业官费以最新公告为准)

---

## 🧠 Claude Code 技能调用矩阵

| 技能标识 | 技能名称 | 说明 | 工具 |
|---------|---------|------|------|
| `[DIALOG]` | 用户交互 | 向用户提问、收集信息、请求确认 | 对话 |
| `[WRITE]` | 文件生成 | 生成申请表、源代码文档、用户手册等 | `Edit`/`Write` |
| `[SHELL]` | Shell 执行 | 提取源代码、统计行数、生成 PDF | `Bash` |
| `[VALIDATE]` | 验证检查 | 对照清单逐项验证材料合规性 | `Read`+分析 |
| `[RESEARCH]` | 知识检索 | 查阅最新政策、费用标准、常见驳回原因 | 内置知识 |
| `[GIT]` | 版本控制 | 软著材料版本管理 | `Bash` |

---

## 📋 总览

```
阶段一: 材料准备 (1-2 天)
├─ Phase 1  信息收集 & 确认
├─ Phase 2  源代码文档 — 提取 → 排版 → PDF
├─ Phase 3  用户手册/设计说明书 — 模板填充 → PDF
├─ Phase 4  权利归属证明 + 申请表预填
└─ Phase 5  五维度核验 (一致性/格式/内容/签章/交又)

阶段二: 在线申请 (0.5 天)
├─ Phase 6  实名认证 → 在线填写 → 上传 → 提交 → 缴费

阶段三: 审查等待 (约 30 个工作日)
├─ Phase 7  受理 → 补正应对 → 公告 → 发证

阶段四: 归档
└─ Phase 8  证书归档 + 使用场景 + 维护
```

---

## ⚠️ 执行前提

```
[DIALOG] 向用户确认:

1. 申请人类型:
   [ ] 个人 — 需身份证正反面扫描件
   [ ] 企业 — 需营业执照扫描件 + 加盖公章

2. 软件类型:
   [ ] iOS App      [ ] Android App     [ ] 小程序
   [ ] Web 前端     [ ] Web 后端        [ ] 桌面应用
   [ ] 全栈 (前端+后端) → 选择其中一个代码库提交

3. 源代码状况:
   [ ] ≥ 3000 行 (去掉空行/注释/第三方库后也 ≥ 3000) → 正常
   [ ] < 3000 行 → 需全部提交 (最低建议 1000 行以上, 太少可能被质疑)

4. 开发方式:
   [ ] 独立开发     [ ] 合作开发 (需协议)
   [ ] 委托开发     [ ] 职务作品 (需单位证明)
   [ ] 基于开源项目修改 → 需声明原创性

5. 是否已发表:
   [ ] 已发表 (有 App Store/应用商店 上架) → 提供首次发表日期
   [ ] 未发表 → 填 "未发表"
```

> **任一信息不明确 → 立即停止，获得用户确认后再继续。**

---

# 📋 阶段一: 材料准备

---

## Phase 1: 信息收集 & 确认

> **[DIALOG]** + **[WRITE]** 收集全部申请所需信息，存入 `COPYRIGHT_INFO.md`

### Step 1.1 — 完整信息收集表

Claude Code 逐项确认并填写:

```markdown
# 软著申请信息表 — ${软件全称}

## 申请人
| 字段 | 内容 |
|------|------|
| 类型 | [个人 / 企业] |
| 名称 | [个人姓名 或 企业全称(与营业执照一致)] |
| 证件类型 | [身份证 / 统一社会信用代码] |
| 证件号码 | [xxxxxxxx] |
| 详细地址 | [省 市 区 街道 门牌号] |
| 邮编 | [6位] |
| 联系人 | [姓名] |
| 手机 | [11位] |
| 邮箱 | [xxx@xxx.com] |

## 软件基本信息
| 字段 | 内容 | 说明 |
|------|------|------|
| 软件全称 | [品牌+功能+软件/系统/平台/应用 + V版本] | **最核心字段, 驳回高发区** |
| 软件简称 | [简称为空时填 "无"] | 不可与全称相同 |
| 版本号 | [V1.0.0] | 与代码中版本一致 |
| 分类号 | [见附表] | 选最接近的 |
| 原创性 | [原创 / 修改] | 基于他人软件修改必须声明 |
| 开发完成日期 | [yyyy-mm-dd] | **不能晚于申请日** |
| 首次发表日期 | [yyyy-mm-dd / 未发表] | 已上架填上架日期 |
| 开发方式 | [独立/合作/委托/职务] | |
| 权利范围 | [全部] | |

## 技术信息
| 字段 | 内容 |
|------|------|
| 编程语言 | [Swift 5.10 / Kotlin 1.9 / Python 3.12 ...] |
| 开发工具/IDE | [Xcode 16 / Android Studio / VS Code ...] |
| 硬件环境 | [如: iPhone 12+, 6GB RAM, 256GB 存储] |
| 软件环境 | [如: iOS 17.0+, watchOS 10.0+] |
| 代码总行数 | [Phase 2 统计后填入] |
| 开发目的 | [如: 帮助用户高效管理日常任务] |
| 面向领域 | [如: 个人生产力/企业管理/教育...] |

## 软件分类号对照表
| 分类号 | 类别 | 典型 App |
|--------|------|---------|
| 30200-0000 | 操作系统 | — |
| 30206-0000 | 企业管理软件 | OA/CRM/ERP |
| 30207-0000 | 行业应用软件 | 金融/医疗/教育 |
| 30208-0000 | 游戏软件 | 手游 |
| 30209-0000 | 多媒体应用软件 | 视频/音频播放器 |
| 30212-0000 | 人工智能软件 | AI 聊天/CV |
| 30213-0000 | 移动应用软件 | **大部分 App 选这个** |
| 30214-0000 | 小程序 | 微信/支付宝小程序 |
| 30900-0000 | 其他 | 不在上述分类中 |
```

### Step 1.2 — 软件命名规范校验

> **[VALIDATE]** 名称不规范 = 100% 驳回

```
✅ 正确格式: "[品牌/产品名] + [功能描述] + [软件/系统/平台/应用] + V[版本号]"

   ✓ "闪记智能笔记管理软件V1.0"
   ✓ "无忧日程任务管理系统V2.1"
   ✓ "蓝鲸数据可视化分析平台V3.0"

❌ 直接驳回:
   - 名称含 "中国" "国家" "中华" "全国" 等 → 需国家授权
   - 过于宽泛: "管理系统" "工具软件" → 缺乏辨识度
   - 个人申请含 "公司" "集团" "企业" → 与主体不符
   - 含特殊符号: © ® ™ → 系统不接受
   - 简称与全称相同 → 必须不同或填 "无"
```

### Step 1.3 — 软件平台选择指南

> **[DIALOG]** 帮用户选择提交哪个代码库

```
App 涉及多端代码时，选择一个代码库提交:

场景1: iOS App + 后端服务
  → 提交 iOS App 源代码 (Swift/SwiftUI 代码更完整、有 UI 可展示)
  → 软件环境写: "iOS 17.0+, 服务器端 CentOS 7+"

场景2: 小程序 + 云函数
  → 提交小程序前端代码 (用户可感知的交互逻辑)
  → 软件环境写: "微信 8.0+, 云开发环境"

场景3: 纯后端 API
  → 提交后端代码 + 附 API 接口文档作为用户手册
  → 软件分类选 "30900-0000 其他"

场景4: Web 全栈 (前端+后端)
  → 提交前端代码 (可视化, 便于审查)
  → 服务器端代码可用附录形式附加

原则: 优先提交客户端代码 (有界面、可展示、逻辑完整)
```

---

## Phase 2: 源代码文档 — 提取 → 排版 → PDF

> **[SHELL]** + **[WRITE]** Claude Code 处理源代码，生成版权局格式的 PDF
> 
> **最关键的步骤。源代码文档不合格 = 100% 驳回。**

### Step 2.1 — 版权局源代码文档规范

```
┌──────────────────────────────────────────────────────┐
│              源代码文档格式要求 (不可偏离)               │
├──────────────────────────────────────────────────────┤
│ 总量:    ≥ 3000 行 (不足则全部提交)                     │
│ 结构:    前 30 页 (连续从程序开头取)                     │
│          + 后 30 页 (连续从程序结尾取)                   │
│          每页 ≥ 50 行 → 合计 ≥ 3000 行                  │
│ 编号:    每页左上角 页眉: "软件名称 + 版本号"             │
│          每页右上角 页眉: "第 X 页 / 共 Y 页"            │
│ 字体:    宋体 (SimSun), 五号 (10.5pt)                   │
│ 行距:    1.5 倍                                         │
│ 页边距:  上 2.5cm  下 2.5cm  左 2.5cm  右 2cm           │
│ 格式:    必须 PDF                                        │
│ 内容:    每行有行号 (或用实际代码)                        │
│          不同源文件之间用 "// ===== 文件名: xxx.swift =====" 分隔 │
│          保留 import 语句和关键注释                       │
│          去除第三方库代码 (Pods/node_modules/.build)       │
└──────────────────────────────────────────────────────┘
```

### Step 2.2 — 正确提取源代码

> **[SHELL]** **不要**像旧版那样 `cat` 所有文件再截取。正确做法是保持文件结构，取文件列表的前段和后段。

```bash
# [SHELL] 定义路径 — 用户提供
SOURCE_DIR="/path/to/your/project/source"   # ← 改为实际路径
OUTPUT_DIR="./copyright_materials"
mkdir -p ${OUTPUT_DIR}

# [SHELL] Step 1: 获取所有源文件 (排除第三方库/构建产物)
SOURCE_FILES=$(find ${SOURCE_DIR} -type f \
    \( -name "*.swift" -o -name "*.m" -o -name "*.h" -o -name "*.c" \
       -o -name "*.cpp" -o -name "*.java" -o -name "*.kt" \
       -o -name "*.js" -o -name "*.ts" -o -name "*.tsx" \
       -o -name "*.py" -o -name "*.dart" -o -name "*.vue" \) \
    -not -path "*/Pods/*" \
    -not -path "*/.build/*" \
    -not -path "*/node_modules/*" \
    -not -path "*/DerivedData/*" \
    -not -path "*/.git/*" \
    -not -path "*/build/*" \
    -not -path "*/Carthage/*" \
    -not -path "*/vendor/*" \
    -not -path "*/Tests/*" \
    -not -path "*/UITests/*" \
    | sort)

FILE_COUNT=$(echo "$SOURCE_FILES" | wc -l)
echo "📄 源文件总数: ${FILE_COUNT}"

# [SHELL] Step 2: 生成带文件标识的完整代码列表 (每个文件前加注释头)
> ${OUTPUT_DIR}/full_source_with_headers.txt
for f in $SOURCE_FILES; do
    # 计算相对路径
    REL_PATH=$(echo "$f" | sed "s|${SOURCE_DIR}/||")
    echo "// ===== 文件: ${REL_PATH} =====" >> ${OUTPUT_DIR}/full_source_with_headers.txt
    # 保留实际代码行 (不删除 import 和注释 — 版权局要的是真实代码)
    grep -v '^\s*$' "$f" >> ${OUTPUT_DIR}/full_source_with_headers.txt || true
    echo "" >> ${OUTPUT_DIR}/full_source_with_headers.txt
done

TOTAL=$(wc -l < ${OUTPUT_DIR}/full_source_with_headers.txt)
echo "📊 总行数 (含文件标识): ${TOTAL}"

# [SHELL] Step 3: 计算需要多少页 (每页50行, 最少前30页+后30页=60页=3000行)
if [ ${TOTAL} -lt 3000 ]; then
    echo "⚠️ 总行数不足 3000, 将提交全部代码 (${TOTAL} 行)"
    PAGES=$(( (TOTAL + 49) / 50 ))  # 向上取整
    cp ${OUTPUT_DIR}/full_source_with_headers.txt ${OUTPUT_DIR}/source_final.txt
else
    # 取前30页 (前1500行) 和 后30页 (后1500行)
    head -1500 ${OUTPUT_DIR}/full_source_with_headers.txt > ${OUTPUT_DIR}/source_front.txt
    tail -1500 ${OUTPUT_DIR}/full_source_with_headers.txt > ${OUTPUT_DIR}/source_back.txt
    cat ${OUTPUT_DIR}/source_front.txt ${OUTPUT_DIR}/source_back.txt > ${OUTPUT_DIR}/source_final.txt
    PAGES=60
fi

FINAL_LINES=$(wc -l < ${OUTPUT_DIR}/source_final.txt)
echo "✅ 最终提交: ${FINAL_LINES} 行, ${PAGES} 页"
```

### Step 2.3 — 生成 PDF (含页眉页码)

> **[WRITE]** Claude Code 创建 Python 脚本生成符合版权局格式的 PDF

```python
# [WRITE] copyright_materials/generate_source_pdf.py
import sys
import os

# 需要: pip install reportlab (如未安装, Claude Code 先 pip install)
from reportlab.lib.pagesizes import A4
from reportlab.lib.units import cm
from reportlab.pdfbase import pdfmetrics
from reportlab.pdfbase.ttfonts import TTFont
from reportlab.platypus import SimpleDocTemplate, Paragraph, PageBreak
from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
from reportlab.platypus.frames import Frame
from reportlab.platypus.doctemplate import PageTemplate

# 配置
SOFTWARE_NAME = "你的软件全称V1.0"   # ← 从 COPYRIGHT_INFO.md 复制
VERSION = "V1.0"                      # ← 从 COPYRIGHT_INFO.md 复制
INPUT_FILE = "source_final.txt"
OUTPUT_FILE = "source_code.pdf"
LINES_PER_PAGE = 50

# 注册中文字体 (macOS 自带宋体)
try:
    pdfmetrics.registerFont(TTFont('SimSun', '/System/Library/Fonts/Songti.ttc', subfontIndex=0))
    CN_FONT = 'SimSun'
except:
    try:
        pdfmetrics.registerFont(TTFont('SimSun', '/System/Library/Fonts/STSong.ttf'))
        CN_FONT = 'SimSun'
    except:
        print("⚠️ 未找到宋体字体, 使用默认字体")
        CN_FONT = 'Helvetica'

# 读取源代码
with open(INPUT_FILE, 'r', encoding='utf-8', errors='replace') as f:
    lines = f.readlines()

total_lines = len(lines)
total_pages = (total_lines + LINES_PER_PAGE - 1) // LINES_PER_PAGE
print(f"📊 {total_lines} 行 → {total_pages} 页")

# 样式 (宋体 五号 ≈ 10.5pt, 1.5倍行距)
style = ParagraphStyle(
    'code',
    fontName=CN_FONT,
    fontSize=10.5,
    leading=10.5 * 1.5,       # 1.5倍行距
    leftIndent=0,
    rightIndent=0,
    spaceBefore=0,
    spaceAfter=0,
)

# 页眉回调
def add_header_footer(canvas, doc):
    canvas.saveState()
    canvas.setFont(CN_FONT, 8)
    # 左上: 软件名称+版本号
    canvas.drawString(2.5*cm, A4[1] - 1.5*cm, f"{SOFTWARE_NAME} {VERSION}")
    # 右上: 页码
    canvas.drawRightString(A4[0] - 2*cm, A4[1] - 1.5*cm, f"第 {canvas.getPageNumber()} 页 / 共 {total_pages} 页")
    canvas.restoreState()

# 构建 PDF
doc = SimpleDocTemplate(
    OUTPUT_FILE,
    pagesize=A4,
    topMargin=2.5*cm,
    bottomMargin=2.5*cm,
    leftMargin=2.5*cm,
    rightMargin=2*cm,
)

# 分页
story = []
for i in range(0, total_lines, LINES_PER_PAGE):
    page_lines = lines[i:i + LINES_PER_PAGE]
    page_text = '<br/>'.join(
        line.rstrip('\n').replace('&', '&amp;').replace('<', '&lt;').replace('>', '&gt;')
        for line in page_lines
    )
    story.append(Paragraph(page_text, style))
    if i + LINES_PER_PAGE < total_lines:
        story.append(PageBreak())

doc.build(story, onFirstPage=add_header_footer, onLaterPages=add_header_footer)
print(f"✅ PDF 已生成: {OUTPUT_FILE}")
print(f"   文件: {OUTPUT_FILE}  ({total_pages} 页)")
```

**执行 PDF 生成**:

```bash
# [SHELL] 安装 reportlab (如未安装)
pip3 install reportlab 2>/dev/null || pip install reportlab

# [SHELL] 执行 PDF 生成
cd ${OUTPUT_DIR}
python3 generate_source_pdf.py

# [SHELL] 验证
ls -lh source_code.pdf
echo "📄 PDF 已生成, 请打开验证格式是否正确"
```

### Step 2.4 — 源代码文档质量检查

> **[VALIDATE]** Claude Code 逐项验证

```
源代码文档质量检查:
□ PDF 页数 ≥ 60 页 (或全部提交时 ≥ (总行数/50) 页)
□ 每页左上角有 "软件名称 + 版本号"
□ 每页右上角有 "第 X 页 / 共 Y 页"
□ 包含程序入口文件 (App.swift / main() / index.js 等)
□ 包含程序结尾 (程序结束逻辑 / 最后的 class / 导出语句)
□ 包含核心业务逻辑 (不是只有 UI 框架代码)
□ 不同文件之间有分隔标识
□ 无第三方库代码 (Pods/node_modules 已排除)
□ 无敏感信息 (密码/密钥/token/IP 地址已移除或脱敏)
□ 代码中版本号与申请版本号一致
□ 打开 PDF 可正常阅读, 无乱码
```

---

## Phase 3: 用户手册/设计说明书 — 模板填充 → PDF

> **[WRITE]** Claude Code 生成用户手册。如 App 无独立用户手册，可提交 **设计说明书** (版权局接受)。

### Step 3.1 — 用户手册模板

```markdown
# ${软件全称} 用户操作手册

## 1. 引言
### 1.1 编写目的
帮助用户了解 ${软件全称} 的功能、安装方法和操作步骤。

### 1.2 适用范围
适用于 ${软件全称} V${版本号}。

### 1.3 术语表
| 术语 | 说明 |
|------|------|
| [术语] | [解释] |

## 2. 软件概述
### 2.1 简介
${软件全称} 是一款面向 [目标用户] 的 [功能描述]。

### 2.2 核心功能
| 编号 | 功能 | 描述 |
|------|------|------|
| 1 | [功能1] | [一句话描述] |
| 2 | [功能2] | [一句话描述] |
| 3 | [功能3] | [一句话描述] |

### 2.3 运行环境
| 环境 | 要求 |
|------|------|
| 操作系统 | [iOS 17.0+] |
| 硬件 | [iPhone 12+, RAM 6GB+] |
| 网络 | [WiFi / 4G/5G 网络] |
| 依赖 | [系统相机权限 / 定位权限] |

## 3. 安装与卸载
### 3.1 安装
1. 打开 [App Store / 应用商店]
2. 搜索 "${软件简称}"
3. 点击 "获取" 或 "安装"
4. 等待下载完成, 图标出现在桌面

### 3.2 卸载
1. 长按桌面 App 图标
2. 点击 "删除 App" → 确认

## 4. 功能操作说明 (本部分每功能配 1-2 张截图)
### 4.1 首页
[截图: 首页界面]
说明: 首页展示 [核心内容]。点击 [xx按钮] 可进入 [xx功能]。

### 4.2 [功能1]
[截图: 功能1界面]
操作步骤:
1. 点击 [xx] 进入功能1
2. [操作描述]
3. [操作结果]

### 4.3 [功能2]
[截图: 功能2界面]
操作步骤:
...

### 4.4 [功能3]
[截图: 功能3界面]
操作步骤:
...

### 4.5 设置
[截图: 设置界面]
说明: 可配置 [偏好设置/账号管理/关于等]。

## 5. 常见问题
| 问题 | 解决方法 |
|------|---------|
| [问题1] | [解决步骤] |
| [问题2] | [解决步骤] |

## 6. 联系与支持
- 邮箱: [xxx@xxx.com]
- 网站: [https://xxx.com]
```

### Step 3.2 — 截图清单 & 获取

```
最少需要 5 张截图 (建议 8-10 张):

□ 首页/主界面          ← 必须
□ 核心功能界面 1        ← 必须
□ 核心功能界面 2        ← 必须
□ 核心功能界面 3        ← 必须
□ 设置/关于页面         ← 必须
□ 操作结果展示 (成功/失败)
□ 登录/注册 (如适用)
□ 空状态页面 (首次使用)

截图要求:
- 分辨率 ≥ 1284×2778 (iPhone 规格)
- 无个人信息泄露 (打码)
- 界面语言为中文 (或中英双语)
- 插入 Word/Pages 后导出为 PDF
```

### Step 3.3 — 用户手册转 PDF

```bash
# [SHELL] 方法 1: 使用 pandoc (推荐)
pandoc user_manual.md -o user_manual.pdf \
  --pdf-engine=xelatex \
  -V mainfont="Songti SC" \
  -V geometry:margin=2.5cm

# 方法 2: Claude Code 用 Python reportlab 生成 (同 Phase 2.3 脚本)
# 方法 3: 用户手动用 Word/Pages 排版后导出 PDF (最简单)
```

### Step 3.4 — 设计说明书 (替代方案)

> 对于纯后端/API 服务，或没有图形界面的软件，提交 **设计说明书** 替代用户手册。

```markdown
# ${软件全称} 设计说明书

## 1. 引言
### 1.1 编写目的
### 1.2 项目背景
### 1.3 术语定义

## 2. 总体设计
### 2.1 系统架构图
[ASCII 架构图或图片]
客户端 → API Gateway → 业务服务 → 数据库

### 2.2 技术选型
| 层级 | 技术 |
|------|------|
| 前端 | SwiftUI |
| 后端 | Vapor 4 |
| 数据库 | PostgreSQL |
| 缓存 | Redis |

### 2.3 模块划分
| 模块 | 功能 | 文件 |
|------|------|------|
| 用户模块 | 注册/登录/资料 | UserService.swift |
| 业务模块 | 核心逻辑 | TaskService.swift |
| 数据模块 | 存储/缓存 | StorageService.swift |

## 3. 详细设计
### 3.1 [模块1] 详细设计
- 功能描述
- 输入/输出
- 流程图/时序图 (或文字描述)
- 关键代码片段 (可选)

### 3.2 [模块2] 详细设计
...

## 4. 接口说明 (如 API)
| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 登录 | POST | /api/login | 用户认证 |
| 获取列表 | GET | /api/tasks | 返回任务列表 |

## 5. 数据库设计 (如有)
[表结构说明]
```

---

## Phase 4: 权利归属证明 & 申请表

> **[WRITE]** 生成权利文件，预填申请表字段

### Step 4.1 — 权利归属证明 (三选一)

**独立开发 — 个人:**

```markdown
# 独立开发声明

本人 [姓名] (身份证号: [xxxxxxxx]) 郑重声明:
${软件全称} V${版本号} 系本人独立开发完成，未抄袭、剽窃他人作品。
本人对该软件享有完整著作权，并承担由此产生的一切法律责任。

声明人 (手写签名): _______________
日期 (手写):     _______________

注: 打印后手写签名, 扫描为 PDF 上传
```

**合作开发 — 多人:**

```markdown
# 合作开发协议

协议各方:
甲方: [姓名1] (身份证号: [xxx])   贡献: [描述]
乙方: [姓名2] (身份证号: [xxx])   贡献: [描述]

各方就 ${软件全称} V${版本号} 著作权归属约定:
1. 著作权由 [甲方单独所有 / 甲乙双方共同所有]
2. 收益分配: [甲方xx% 乙方xx%]
3. 登记申请由 [甲方/乙方] 作为申请人办理

甲方 (手写签名): _______________  日期: _______________
乙方 (手写签名): _______________  日期: _______________
```

**职务作品 — 企业:**

```markdown
# 职务作品确认书

本单位 [企业全称] (统一社会信用代码: [xxxxxxxx]) 确认:
员工 [姓名] (身份证号: [xxx]) 在任职期间 (yyyy-mm-dd 至 yyyy-mm-dd)
开发的 ${软件全称} V${版本号}，系为完成本单位工作任务所创作的职务作品。

依据《著作权法》第十八条，该软件著作权归本单位所有。

单位 (公章): _______________
法定代表人/授权人 (签字): _______________
日期: _______________

注: 必须加盖单位公章 (圆形公章, 非部门章/合同章)
```

### Step 4.2 — 申请表完整字段 (在线填写的参照)

> **[WRITE]** Claude Code 生成 `APPLICATION_FORM.md` 供用户逐字段复制粘贴

```markdown
# 申请表字段对照 — 在线填写时复制此文件

## 软件基本信息
- 软件全称: [从 COPYRIGHT_INFO.md 复制 — 不可有任何差异]
- 软件简称: [从 COPYRIGHT_INFO.md 复制]
- 版本号:   [从 COPYRIGHT_INFO.md 复制]
- 分类号:   [从 COPYRIGHT_INFO.md 选择]
- 原创性:   [原创 / 修改]
  (如是修改: 需说明原软件名称、登记号、修改内容)

## 开发信息
- 开发完成日期: [yyyy-mm-dd]
- 首次发表日期: [yyyy-mm-dd / 未发表]
- 开发方式:     [独立/合作/委托/职务]
- 硬件环境:     [如: iPhone 12及以上机型, 6GB RAM, 256GB存储]
- 软件环境:     [如: iOS 17.0及以上, 服务器端 CentOS 7+]
- 编程语言:     [如: Swift 5.10, Markdown, JSON]
- 代码行数:     [填入 Phase 2 统计的总行数]
- 开发目的:     [如: 为用户提供高效的任务管理与日程规划功能]
- 面向领域:     [如: 个人生产力 / 企业管理]

## 申请人信息
- 姓名/名称: [与证件完全一致]
- 证件类型: [身份证 / 统一社会信用代码]
- 证件号:   [xxxxxxxx]
- 地址:     [省 市 区 街道 门牌号]
- 邮编:     [6位]
- 代理人:   [无 / 有 (如有需代理委托书)]

## 权利范围
- 权利范围: [全部] — 99%情况选此项
```

---

## Phase 5: 五维度材料核验

> **[VALIDATE]** 提交前最后一道防线

### 维度一: 一致性 (不一致 = 100% 驳回)

```
□ 软件全称:  申请表 = 源代码PDF页眉 = 用户手册封面 = COPYRIGHT_INFO ✅
□ 版本号:    申请表 = 源代码PDF页眉 = 用户手册封面 = COPYRIGHT_INFO ✅
□ 申请人:    申请表 = 证件 = 权利归属文件 ✅
□ 开发日期:  申请表 = 所有材料标注一致 ✅
```

### 维度二: 格式

```
□ 源代码 PDF:  页眉/页码/宋体五号/1.5行距/边距正确/≥60页 ✅
□ 用户手册 PDF: 封面/目录/页码/截图清晰/15-30页 ✅
□ 证件扫描件:  清晰/四角完整/在有效期内/无水印遮挡 ✅
□ 权利证明:    有手写签名/公章/日期 ✅
□ 所有文件 ≤ 30MB ✅
```

### 维度三: 内容

```
□ 源代码有开头 (App 入口/程序启动) ✅
□ 源代码有结尾 (程序结束逻辑/最后的模块导出) ✅
□ 源代码有核心业务 (不是只有空壳/框架代码) ✅
□ 用户手册 ≥ 5 张实际运行截图 ✅
□ 用户手册操作步骤可执行 (不是描述性文字堆砌) ✅
□ 无敏感信息泄露 (密码/密钥/token/IP/手机号) ✅
```

### 维度四: 签章

```
□ 个人: 身份证正反面扫描件 + 申请表在线电子签名
□ 企业: 营业执照扫描件 + 申请表加盖公章 (扫描件或电子章)
□ 权利文件签名/盖章齐全
```

### 维度五: 交叉验证

```
□ 软件名称不含禁用词 (Phase 1.2 已检查) ✅
□ 开发完成日期 ≤ 申请日期 (不能在未来) ✅
□ 首次发表日期 ≥ 开发完成日期 (不能先发表后完成) ✅
□ 个人申请: 名称不含 "公司/集团/企业" ✅
□ 企业申请: 申请人名称 = 营业执照全称 (逐字一致) ✅
□ 申请表中代码行数 = 源代码 PDF 实际行数 ✅
```

> **Claude Code 执行**: `git add -A && git commit -m "软著申请材料准备完成 — 五维度核验通过"`

---

# 📋 阶段二: 在线申请

---

## Phase 6: 在线提交

> **[DIALOG]** Claude Code 逐步指导用户操作

### Step 6.1 — 账号注册 & 实名认证

```
═══════════════════════════════════════
📱 第一步: 注册 & 实名认证
═══════════════════════════════════════

1. 访问: https://www.ccopyright.com.cn
   (中国版权保护中心 — 官方唯一渠道)

2. 注册:
   - 点击 "注册" → 选择用户类型
   - 个人: 身份证号 + 手机号 + 人脸识别
   - 企业: 统一社会信用代码 + 法人信息 + 对公账户验证

3. 实名认证 (关键! 未认证无法申请):
   - 提交后等待 1-3 个工作日审核
   - 审核通过后账号状态显示 "已认证"
   - ⚠️ 如在 Step 0 发现未认证，先完成此步再准备材料

4. 登录:
   - 使用已认证账号登录
```

### Step 6.2 — 在线填写申请表

```
═══════════════════════════════════════
📝 第二步: 填写申请表
═══════════════════════════════════════

1. 登录 → "计算机软件著作权登记" → "R11 计算机软件著作权登记申请"
2. 逐字段复制 APPLICATION_FORM.md 中的内容
3. 重点:
   - 每个字段填写后立即检查 ← → 与 APPLICATION_FORM.md 比对
   - "软件全称" 不能有任何多余空格或标点差异
   - "代码行数" 必须与源代码 PDF 实际行数一致
4. 填写完成 → 点击 "保存" → 点击 "下一步"
```

### Step 6.3 — 上传附件

```
═══════════════════════════════════════
📎 第三步: 上传材料
═══════════════════════════════════════

按系统引导依次上传:
□ 1. 源程序文档       → copyright_materials/source_code.pdf
□ 2. 用户操作手册      → copyright_materials/user_manual.pdf
□ 3. 申请人身份证明    → 身份证正反面 / 营业执照 扫描件
□ 4. 权利归属证明文件  → (如有) 按 Phase 4.1 生成的 PDF
□ 5. 其他证明文件      → (如有合作开发协议/委托书等)

上传后系统会显示文件页数, 与预期核对。
```

### Step 6.4 — 提交 & 获取受理号

```
═══════════════════════════════════════
✅ 第四步: 提交
═══════════════════════════════════════

1. 确认所有材料已上传 → 预览申请表 → 确认无误
2. 点击 "提交"
3. 系统生成受理号 (如: R11-2026-00012345)
   → 📸 截图保存受理号!
4. 下载/打印申请表 (PDF)
   - 个人: 在申请表上签名
   - 企业: 在申请表上加盖公章
5. 状态变为 "待受理"
```

### Step 6.5 — 缴费 (如需)

```
═══════════════════════════════════════
💰 第五步: 缴费
═══════════════════════════════════════

当前政策 (以官方最新公告为准):
- 个人申请: 免费 (自 2023 年相关政策调整)
- 企业申请: 以系统提示金额为准

缴费后保留凭证 → 状态变为 "已缴费, 待受理"
```

---

# 📋 阶段三: 审查追踪

---

## Phase 7: 受理 → 补正 → 公告 → 发证

> **[DIALOG]** Claude Code 定期提醒用户检查进度

### 系统状态流转

```
提交 → 待受理 → 已受理 (3-5工作日)
                    ↓
              审查中 (15-20工作日)
                    ↓
         ┌─────────┴─────────┐
         ↓                   ↓
      审查通过              待补正 (收到通知后30日内补正)
         ↓                   ↓
      已公告 (5工作日)      补正提交 → 重新审查
         ↓
      已发证 → 下载电子证书
```

### Step 7.1 — 受理阶段

```
提交后 3-5 个工作日:
□ 登录系统查看 → 状态 "已受理" → ✅ 进入审查
□ 状态仍为 "待受理" → 继续等待 (最多 7 个工作日)
□ 状态变为 "已退回" → 查看退回原因 → 修改 → 重新提交
□ 常见退回原因:
  - 实名认证未完成 → 先完成认证
  - 材料格式不符合要求 → 修改 PDF
  - 名称违规 → 修改软件名称
```

### Step 7.2 — 补正应对 (如触发)

```
如果收到 "补正通知书":

1. [READ] 下载补正通知书, 仔细阅读补正要求
2. 常见补正场景 & 应对:

   ┌─────────────────────┬──────────────────────────┐
   │ 补正原因              │ 操作                      │
   ├─────────────────────┼──────────────────────────┤
   │ 源代码缺少核心模块     │ 从 Phase 2 重提取, 确认   │
   │                      │ 包含主要业务逻辑文件        │
   ├─────────────────────┼──────────────────────────┤
   │ 源代码无程序结尾       │ 补充最后几个文件的代码,     │
   │                      │ 确保包含程序结尾逻辑        │
   ├─────────────────────┼──────────────────────────┤
   │ 用户手册截图不清晰     │ 换高清截图 → 重新导出 PDF   │
   ├─────────────────────┼──────────────────────────┤
   │ 软件名称不符合规范     │ 按审查意见修改名称          │
   │                      │ ⚠️ 所有材料中的名称都要改!   │
   ├─────────────────────┼──────────────────────────┤
   │ 权利归属证明不完整     │ 补充签名/盖章/日期          │
   ├─────────────────────┼──────────────────────────┤
   │ 源代码与申请版本不一致  │ 确认代码版本 = 申请版本号    │
   └─────────────────────┴──────────────────────────┘

3. [WRITE] 修改材料 → [VALIDATE] 重新核验
4. 在系统中上传补正材料 → 提交
5. 重新进入审查队列 (补正后 15 个工作日)
```

### Step 7.3 — 公告 & 发证

```
审查通过后:
□ 状态变为 "已公告" → 公告期 5 个工作日
□ 公告期结束 → 状态变为 "已发证"
□ 登录系统 → 下载 "计算机软件著作权登记证书" (电子版 PDF)
□ 证书包含: 软件名称、著作权人、登记号、发证日期
□ 核对证书信息与申请信息一致

纸质证书:
□ 如需纸质版 → 在系统中申请邮寄 (填写地址)
□ 邮寄时间: 约 7-15 个工作日
□ 目前电子证书与纸质证书具有同等法律效力
```

---

# 📋 阶段四: 归档

---

## Phase 8: 证书归档 & 后续

> **[GIT]** 归档证书和申请材料

### Step 8.1 — 归档

```bash
# [SHELL] 归档到项目 docs 目录
mkdir -p ./docs/copyright
cp copyright_materials/source_code.pdf ./docs/copyright/
cp copyright_materials/user_manual.pdf ./docs/copyright/
cp 软著登记证书.pdf ./docs/copyright/certificate.pdf
cp COPYRIGHT_INFO.md ./docs/copyright/
cp APPLICATION_FORM.md ./docs/copyright/

# [GIT] 提交 (⚠️ 注意: 证书含个人信息, 确认仓库是否公开)
git add docs/copyright/
git commit -m "Add software copyright certificate v1.0"
```

### Step 8.2 — 证书验证

```
电子证书真伪验证:
1. 扫描证书上的二维码 → 跳转到官方验证页面
2. 或访问: https://www.ccopyright.com.cn → 证书查询
3. 输入登记号 → 查询 → 核对信息
```

### Step 8.3 — 软著使用场景

```
用途:
□ App Store 上架 → 知识产权证明
□ 国内应用商店 (华为/小米/OPPO/vivo/应用宝) → 上架必备
□ 高新技术企业认定 → 核心知识产权
□ 双软认证 → 必备材料
□ 项目申报/政府补贴 → 知识产权佐证
□ 法律维权 → 权属证明文件
```

### Step 8.4 — 版本维护

```
□ 大版本升级 (V1.0 → V2.0): 建议重新申请 (代码变化大)
□ 小版本更新 (V1.0 → V1.1): 原登记有效, 无需重新申请
□ 著作权转让: 办理 "著作权转让登记" (需转让合同)
□ 权利人更名: 办理 "著作权人名称变更"
□ 证书遗失: 可申请补发
□ 有效期: 个人 → 终身+死后50年; 单位 → 首次发表后50年
```

---

## 📚 附录

### A. 版权局官方渠道

| 渠道 | 地址/方式 |
|------|---------|
| 官网 | https://www.ccopyright.com.cn |
| 电话 | 010-68003887 (工作日 9:00-17:00) |
| 线下大厅 | 北京市西城区天桥南大街1号天桥艺术大厦A座 |

### B. 常见驳回原因 & SOP 防御

| 驳回原因 | 频率 | SOP 防御位置 |
|---------|------|-------------|
| 源代码不完整/无核心逻辑 | ★★★★★ | Phase 2.4 质量检查 |
| 软件名称不合规 | ★★★★☆ | Phase 1.2 命名校验 |
| 申请表与附件名称/版本不一致 | ★★★★☆ | Phase 5 一致性维 |
| 用户手册无截图/截图太少 | ★★★★☆ | Phase 3.2 截图清单 |
| 源代码无文件分隔/缺少开头结尾 | ★★★☆☆ | Phase 2.2 文件标识+2.4 检查 |
| 证件模糊/过期 | ★★★☆☆ | Phase 5 格式维 |
| 权利归属不清/无证明 | ★★☆☆☆ | Phase 4.1 |
| 实名认证未完成 | ★★☆☆☆ | Phase 6.1 |
| 分类号选错 | ★★☆☆☆ | Phase 1.1 |
| 代码行数不达标 | ★★☆☆☆ | Phase 2.2 统计 |

### C. PDF 生成故障排除

```
问题: reportlab 中文字体找不到
解:  检查系统字体: ls /System/Library/Fonts/ | grep -i song
     macOS 自带 Songti.ttc (宋体-简), 脚本已配置

问题: pandoc 导出 PDF 中文乱码
解:  使用 Python reportlab 方案 (Phase 2.3 脚本)

问题: PDF 超过 30MB
解:  - 截图压缩 (pngquant / jpegoptim)
     - 减少用户手册页数 (删冗余内容)
     - 源代码确认每页 55-60 行 (而非刚好 50)

问题: 源代码总行数 < 3000
解:  提交全部代码即可 (不满足 3000 不影响受理,
     但可能被审查员质疑软件规模, 建议补充代码)
```

### D. 软件著作权 vs 其他知识产权

```
┌────────────┬──────────────────┬──────────┬──────────┐
│ 类型        │ 保护对象           │ 审查周期   │ 保护期    │
├────────────┼──────────────────┼──────────┼──────────┤
│ 软件著作权   │ 源代码 + 文档       │ ~30工作日  │ 50年      │
│ 发明专利    │ 技术方案 (算法/方法) │ 1-3年     │ 20年      │
│ 外观专利    │ GUI 设计           │ 6-12月    │ 15年      │
│ 商标       │ App 名称/Logo      │ 6-12月    │ 10年(可续) │
└────────────┴──────────────────┴──────────┴──────────┘

App 保护建议: 软著(必须) + 商标(名称保护) → 先软著后商标
```

---

> **SOP 版本**: 2.0.0
> **最后更新**: 2026-06-08
> **适用范围**: 中国计算机软件著作权登记 (所有软件类型)
> **基于**: 中国版权保护中心最新流程 + 实际办理经验
